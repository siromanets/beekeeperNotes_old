package com.example.key.beekeepernote.login;

import android.app.Application;
import android.arch.lifecycle.AndroidViewModel;
import android.support.annotation.NonNull;

public class SetPasswordFragmentVM extends AndroidViewModel {

    private static final String TAG = "SetPasswordFragmentVM";

    public SetPasswordFragmentVM(@NonNull Application application) {
        super(application);
    }
}
